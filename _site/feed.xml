<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="http://0.0.0.0:4000/feed.xml" rel="self" type="application/atom+xml" /><link href="http://0.0.0.0:4000/" rel="alternate" type="text/html" /><updated>2026-03-13T12:53:29-05:00</updated><id>http://0.0.0.0:4000/feed.xml</id><title type="html">Nguyen Phuc Ngoc Thanh</title><subtitle>This is my blog hosted on GitHub Pages.</subtitle><author><name>Nguyen Phuc Ngoc Thanh</name></author><entry><title type="html">Kiến thức về nguyên tắc SOLID</title><link href="http://0.0.0.0:4000/2025/08/23/solid-post.html" rel="alternate" type="text/html" title="Kiến thức về nguyên tắc SOLID" /><published>2025-08-23T00:00:00-05:00</published><updated>2025-08-23T00:00:00-05:00</updated><id>http://0.0.0.0:4000/2025/08/23/solid-post</id><content type="html" xml:base="http://0.0.0.0:4000/2025/08/23/solid-post.html"><![CDATA[<p>SOLID là viết tắt của 5 nguyên tắc thiết kế giúp code dễ đọc, dễ mở rộng, dễ bảo trì và hạn chế code smell(code xấu).</p>

<ol>
  <li>S – Single Responsibility Principle (SRP)</li>
</ol>

<ul>
  <li>Mỗi class chỉ nên chịu trách nhiệm về một việc duy nhất.</li>
  <li>Nếu một class có nhiều hơn một lý do để thay đổi → vi phạm SRP.</li>
</ul>

<ol>
  <li>O – Open/Closed Principle (OCP)</li>
  <li>L – Liskov Substitution Principle (LSP)</li>
  <li>I – Interface Segregation Principle (ISP)</li>
  <li>D – Dependency Inversion Principle (DIP)</li>
</ol>

<p><strong>Tóm lại:</strong></p>

<ul>
  <li>S: Mỗi class chỉ nên làm 1 việc.</li>
  <li>O: Mở rộng dễ dàng, không sửa code cũ.</li>
  <li>L: Lớp con phải thay thế được lớp cha.</li>
  <li>I: Interface nhỏ gọn, đúng mục đích.</li>
  <li>D: Phụ thuộc vào abstraction, không phụ thuộc implementation.</li>
</ul>]]></content><author><name>Nguyen Phuc Ngoc Thanh</name></author><summary type="html"><![CDATA[SOLID là viết tắt của 5 nguyên tắc thiết kế giúp code dễ đọc, dễ mở rộng, dễ bảo trì và hạn chế code smell(code xấu). S – Single Responsibility Principle (SRP) Mỗi class chỉ nên chịu trách nhiệm về một việc duy nhất. Nếu một class có nhiều hơn một lý do để thay đổi → vi phạm SRP. O – Open/Closed Principle (OCP) L – Liskov Substitution Principle (LSP) I – Interface Segregation Principle (ISP) D – Dependency Inversion Principle (DIP) Tóm lại: S: Mỗi class chỉ nên làm 1 việc. O: Mở rộng dễ dàng, không sửa code cũ. L: Lớp con phải thay thế được lớp cha. I: Interface nhỏ gọn, đúng mục đích. D: Phụ thuộc vào abstraction, không phụ thuộc implementation.]]></summary></entry><entry><title type="html">Ôn tập kiến thức về thuật toán cơ bản</title><link href="http://0.0.0.0:4000/2025/08/17/basic_algorithms-post.html" rel="alternate" type="text/html" title="Ôn tập kiến thức về thuật toán cơ bản" /><published>2025-08-17T00:00:00-05:00</published><updated>2025-08-17T00:00:00-05:00</updated><id>http://0.0.0.0:4000/2025/08/17/basic_algorithms-post</id><content type="html" xml:base="http://0.0.0.0:4000/2025/08/17/basic_algorithms-post.html"><![CDATA[<p><strong>I. Khái niệm nền tảng</strong></p>

<ul>
  <li>Thuật toán (Algorithm): tập hợp hữu hạn các bước để giải quyết một vấn đề.</li>
  <li>Độ phức tạp:
    <ul>
      <li>Time Complexity: thời gian chạy (O(1), O(log n), O(n), O(n log n), O(n²) …).</li>
      <li>Space Complexity: bộ nhớ sử dụng.</li>
      <li>Big-O notation: ký hiệu để biểu diễn độ phức tạp thuật toán theo input size n.</li>
    </ul>
  </li>
</ul>

<p><strong>II. Thuật toán tìm kiếm (Searching)</strong></p>

<p><strong>1. Linear Search (tuần tự)</strong></p>

<ul>
  <li>Khi dùng: mảng/list nhỏ hoặc chưa sắp xếp.</li>
  <li>Độ phức tạp: O(n); bộ nhớ O(1)</li>
  <li>Ý tưởng: duyệt từ trái qua phải, gặp là trả về
Ví dụ: [4, 2, 9, 7] tìm 9 → duyệt 4→2→9 (trả về index 2).</li>
</ul>

<p><strong>2. Binary Search (nhị phân)</strong></p>

<ul>
  <li>Khi dùng: mảng đã sắp xếp (rất quan trọng).</li>
  <li>Độ phức tạp: O(log n); bộ nhớ O(1).</li>
  <li>Ý tưởng: so sánh ở giữa, loại nửa không cần, lặp lại.</li>
  <li>
    <p>Lưu ý biên: vòng lặp while (l &lt;= r), mid = l + (r - l) / 2 để tránh tràn.
Ví dụ: [1,3,5,7,9] tìm 7 → mid=5 &lt; 7 ⇒ xét nửa phải → mid=7 (tìm thấy).</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  public static int binarySearch(int[] a, int target) {
    int l = 0; // biên trái
    int r = a.length - 1; // biên phải

      while (l &lt;= r) {
          int mid = l + (r - l) / 2; // tránh tràn số
          if (a[mid] == target) {
              return mid; // tìm thấy, trả về index
          }
          if (a[mid] &lt; target) {
              l = mid + 1; // bỏ qua nửa trái
          } else {
              r = mid - 1; // bỏ qua nửa phải
          }
      }
      return -1; // không tìm thấy
  }
</code></pre></div>    </div>
  </li>
</ul>

<p><strong>II. Thuật toán tìm kiếm (Searching)</strong></p>

<p><strong>1. Bubble Sort</strong></p>

<ul>
  <li>
    <p>Ý tưởng:</p>

    <ul>
      <li>Duyệt nhiều lần qua mảng.</li>
      <li>Mỗi lần duyệt, so sánh cặp phần tử liền kề, nếu sai thứ tự thì hoán đổi.</li>
      <li>Sau mỗi lượt, phần tử lớn nhất “nổi” lên cuối.</li>
    </ul>
  </li>
  <li>
    <p>Độ phức tạp:</p>

    <ul>
      <li>Trung bình / Tệ nhất: O(n²)</li>
      <li>Tốt nhất (mảng đã sort): O(n)</li>
      <li>Bộ nhớ: O(1), ổn định.</li>
    </ul>
  </li>
  <li>
    <p>Ví dụ:</p>

    <ul>
      <li>Mảng: [5, 1, 4, 2]</li>
      <li>Lượt 1: (5,1) → swap → [1,5,4,2]; (5,4) → [1,4,5,2]; (5,2) → [1,4,2,5]</li>
      <li>
        <p>Lượt 2: (1,4), ok; (4,2) → [1,2,4,5]; (4,5) ok
→ Mảng đã sort.</p>

        <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public static void bubbleSort(int[] a) {
  boolean swapped;
  int n = a.length;
  do {
      swapped = false;
      for (int i = 1; i &lt; n; i++) {
          if (a[i - 1] &gt; a[i]) {
              int tmp = a[i];
              a[i] = a[i - 1];
              a[i - 1] = tmp;
              swapped = true;
          }
      }
      n--; // phần tử cuối đã đúng vị trí
  } while (swapped);
}
</code></pre></div>        </div>
      </li>
    </ul>
  </li>
</ul>

<p><strong>2. Selection Sort</strong></p>

<ul>
  <li>
    <p>Ý tưởng:</p>

    <ul>
      <li>Ở mỗi vòng lặp, tìm phần tử nhỏ nhất trong đoạn chưa sort, đưa nó về đầu.</li>
      <li>Sau vòng thứ i, các phần tử [0..i] đã đúng chỗ.</li>
    </ul>
  </li>
  <li>
    <p>Độ phức tạp:</p>

    <ul>
      <li>Trung bình / Tệ nhất: O(n²)</li>
      <li>Không ổn định, bộ nhớ O(1).</li>
    </ul>
  </li>
  <li>
    <p>Ví dụ:</p>

    <ul>
      <li>Mảng [29, 10, 14, 37]</li>
      <li>Lượt 1: min=10 → swap với 29 → [10,29,14,37]</li>
      <li>
        <p>Lượt 2: min=14 → swap với 29 → [10,14,29,37] → Done.</p>

        <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public static void selectionSort(int[] a) {
    int n = a.length;
    for (int i = 0; i &lt; n - 1; i++) {
        int min = i;
        for (int j = i + 1; j &lt; n; j++) {
            if (a[j] &lt; a[min]) min = j;
        }
        int tmp = a[i];
        a[i] = a[min];
        a[min] = tmp;
    }
}
</code></pre></div>        </div>
      </li>
    </ul>
  </li>
</ul>

<p><strong>3. Insertion Sort</strong></p>

<ul>
  <li>
    <p>Ý tưởng:</p>

    <ul>
      <li>Duyệt từ trái sang phải.</li>
      <li>Với mỗi phần tử, chèn nó vào đúng vị trí trong đoạn đã sort trước đó.</li>
    </ul>
  </li>
  <li>
    <p>Độ phức tạp:</p>

    <ul>
      <li>Trung bình / Tệ nhất: O(n²)</li>
      <li>Tốt nhất (mảng gần như sort): O(n)</li>
      <li>Ổn định, bộ nhớ O(1).</li>
    </ul>
  </li>
  <li>
    <p>Ví dụ:</p>

    <ul>
      <li>Mảng [5, 3, 4]</li>
      <li>5: đoạn đã sort [5].</li>
      <li>3: chèn vào trước 5 → [3,5].</li>
      <li>
        <p>4: chèn giữa 3 và 5 → [3,4,5].</p>

        <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public static void insertionSort(int[] a) {
    for (int i = 1; i &lt; a.length; i++) {
        int key = a[i];
        int j = i - 1;
        while (j &gt;= 0 &amp;&amp; a[j] &gt; key) {
            a[j + 1] = a[j];
            j--;
        }
        a[j + 1] = key;
    }
}
</code></pre></div>        </div>
      </li>
    </ul>
  </li>
</ul>

<p><strong>4. Merge Sort</strong></p>

<ul>
  <li>
    <p>Ý tưởng:</p>

    <ul>
      <li>
        <p>Chia để trị:</p>

        <ul>
          <li>Chia mảng thành 2 nửa.</li>
          <li>Sắp xếp từng nửa đệ quy.</li>
          <li>Gộp (merge) 2 nửa đã sort.</li>
        </ul>
      </li>
    </ul>
  </li>
  <li>
    <p>Độ phức tạp:</p>

    <ul>
      <li>Luôn O(n log n)</li>
      <li>Bộ nhớ: O(n), ổn định.</li>
    </ul>
  </li>
  <li>
    <p>Ví dụ:</p>

    <ul>
      <li>Mảng [38, 27, 43, 3]</li>
      <li>Chia: [38,27] và [43,3].</li>
      <li>Sort trái → [27,38]; sort phải → [3,43].</li>
      <li>
        <p>Merge → [3,27,38,43].</p>

        <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public static void mergeSort(int[] a) {
    if (a.length &lt;= 1) return;
    int[] tmp = new int[a.length];
    mergeSort(a, 0, a.length - 1, tmp);
}

private static void mergeSort(int[] a, int l, int r, int[] tmp) {
    if (l &gt;= r) return;
    int m = l + (r - l) / 2;
    mergeSort(a, l, m, tmp);
    mergeSort(a, m + 1, r, tmp);
    merge(a, l, m, r, tmp);
}

private static void merge(int[] a, int l, int m, int r, int[] tmp) {
    int i = l, j = m + 1, k = l;
    while (i &lt;= m &amp;&amp; j &lt;= r) {
        if (a[i] &lt;= a[j]) tmp[k++] = a[i++];
        else tmp[k++] = a[j++];
    }
    while (i &lt;= m) tmp[k++] = a[i++];
    while (j &lt;= r) tmp[k++] = a[j++];
    for (int t = l; t &lt;= r; t++) a[t] = tmp[t];
}
</code></pre></div>        </div>
      </li>
    </ul>
  </li>
</ul>

<p><strong>5. Quick Sort (chia để trị)</strong></p>

<ul>
  <li>
    <p>Ý tưởng:</p>

    <ul>
      <li>
        <p>Chia để trị:</p>

        <ol>
          <li>Chọn pivot (thường là giữa, trái, phải hoặc ngẫu nhiên).</li>
          <li>Phân chia mảng thành 2 phần: ≤ pivot và ≥ pivot.</li>
          <li>Đệ quy sort 2 nửa.</li>
        </ol>
      </li>
    </ul>
  </li>
  <li>
    <p>Độ phức tạp:</p>

    <ul>
      <li>Trung bình: O(n log n)</li>
      <li>Tệ nhất: O(n²) (pivot xấu).</li>
      <li>Bộ nhớ: O(log n), không ổn định.</li>
    </ul>
  </li>
  <li>
    <p>Ví dụ:</p>

    <ul>
      <li>Mảng [9,3,7,1], pivot=7</li>
      <li>Nhỏ hơn pivot: [3,1], lớn hơn: [9].</li>
      <li>Sort [3,1] → [1,3].</li>
      <li>
        <p>Merge lại: [1,3,7,9].</p>

        <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public static void quickSort(int[] a) {
    quickSort(a, 0, a.length - 1);
}

private static void quickSort(int[] a, int l, int r) {
    if (l &gt;= r) return;
    int pivot = a[l + (r - l) / 2];
    int i = l, j = r;
    while (i &lt;= j) {
        while (a[i] &lt; pivot) i++;
        while (a[j] &gt; pivot) j--;
        if (i &lt;= j) {
            int tmp = a[i]; a[i] = a[j]; a[j] = tmp;
            i++; j--;
        }
    }
    if (l &lt; j) quickSort(a, l, j);
    if (i &lt; r) quickSort(a, i, r);
}
</code></pre></div>        </div>
      </li>
    </ul>
  </li>
</ul>

<p><strong>6. Heap Sort</strong></p>

<ul>
  <li>
    <p>Ý tưởng:</p>

    <ul>
      <li>Xây max-heap từ mảng.</li>
      <li>Lặp: swap root (max) với phần tử cuối, giảm heap-size, heapify lại.</li>
    </ul>
  </li>
  <li>
    <p>Độ phức tạp:</p>

    <ul>
      <li>Luôn O(n log n)</li>
      <li>In-place, không ổn định.</li>
    </ul>
  </li>
  <li>
    <p>Ví dụ:</p>

    <ul>
      <li>Mảng [4,10,3,5,1]</li>
      <li>Heapify → [10,5,3,4,1]</li>
      <li>Swap 10 ↔ 1 → [1,5,3,4,10]</li>
      <li>Heapify → [5,4,3,1,10]</li>
      <li>
        <p>Swap 5 ↔ 1 → [1,4,3,5,10]
→ v.v.</p>

        <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>public static void heapSort(int[] a) {
    int n = a.length;
    for (int i = n / 2 - 1; i &gt;= 0; i--) heapify(a, n, i);
    for (int end = n - 1; end &gt; 0; end--) {
        int tmp = a[0]; a[0] = a[end]; a[end] = tmp;
        heapify(a, end, 0);
    }
}

private static void heapify(int[] a, int n, int i) {
    int largest = i, left = 2*i + 1, right = 2*i + 2;
    if (left &lt; n &amp;&amp; a[left] &gt; a[largest]) largest = left;
    if (right &lt; n &amp;&amp; a[right] &gt; a[largest]) largest = right;
    if (largest != i) {
        int tmp = a[i]; a[i] = a[largest]; a[largest] = tmp;
        heapify(a, n, largest);
    }
}
</code></pre></div>        </div>
      </li>
    </ul>
  </li>
</ul>

<p><strong>7. Counting Sort</strong></p>

<ul>
  <li>Ý tưởng:
    <ul>
      <li>Đếm tần suất xuất hiện của từng số trong [min..max].</li>
      <li>Tính prefix sum để biết vị trí.</li>
      <li>Sắp xếp ổn định bằng cách chèn ngược.</li>
    </ul>
  </li>
  <li>Độ phức tạp:
    <ul>
      <li>O(n + k), k = phạm vi giá trị.</li>
      <li>Bộ nhớ O(n+k), ổn định.</li>
    </ul>
  </li>
</ul>

<p><strong>8. Radix Sort (áp dụng cho dữ liệu số nguyên lớn)</strong></p>

<ul>
  <li>Ý tưởng:
    <ul>
      <li>Sort theo từng chữ số (hàng đơn vị → chục → trăm).</li>
      <li>Dùng Counting Sort ổn định ở mỗi bước.</li>
    </ul>
  </li>
  <li>Độ phức tạp:
    <ul>
      <li>O(d·(n + b)), với d = số chữ số, b = cơ số (10).</li>
      <li>Ổn định, tốt cho số nguyên lớn, không âm.</li>
    </ul>
  </li>
</ul>]]></content><author><name>Nguyen Phuc Ngoc Thanh</name></author><summary type="html"><![CDATA[I. Khái niệm nền tảng Thuật toán (Algorithm): tập hợp hữu hạn các bước để giải quyết một vấn đề. Độ phức tạp: Time Complexity: thời gian chạy (O(1), O(log n), O(n), O(n log n), O(n²) …). Space Complexity: bộ nhớ sử dụng. Big-O notation: ký hiệu để biểu diễn độ phức tạp thuật toán theo input size n. II. Thuật toán tìm kiếm (Searching) 1. Linear Search (tuần tự) Khi dùng: mảng/list nhỏ hoặc chưa sắp xếp. Độ phức tạp: O(n); bộ nhớ O(1) Ý tưởng: duyệt từ trái qua phải, gặp là trả về Ví dụ: [4, 2, 9, 7] tìm 9 → duyệt 4→2→9 (trả về index 2). 2. Binary Search (nhị phân) Khi dùng: mảng đã sắp xếp (rất quan trọng). Độ phức tạp: O(log n); bộ nhớ O(1). Ý tưởng: so sánh ở giữa, loại nửa không cần, lặp lại. Lưu ý biên: vòng lặp while (l &lt;= r), mid = l + (r - l) / 2 để tránh tràn. Ví dụ: [1,3,5,7,9] tìm 7 → mid=5 &lt; 7 ⇒ xét nửa phải → mid=7 (tìm thấy). public static int binarySearch(int[] a, int target) { int l = 0; // biên trái int r = a.length - 1; // biên phải while (l &lt;= r) { int mid = l + (r - l) / 2; // tránh tràn số if (a[mid] == target) { return mid; // tìm thấy, trả về index } if (a[mid] &lt; target) { l = mid + 1; // bỏ qua nửa trái } else { r = mid - 1; // bỏ qua nửa phải } } return -1; // không tìm thấy } II. Thuật toán tìm kiếm (Searching) 1. Bubble Sort Ý tưởng: Duyệt nhiều lần qua mảng. Mỗi lần duyệt, so sánh cặp phần tử liền kề, nếu sai thứ tự thì hoán đổi. Sau mỗi lượt, phần tử lớn nhất “nổi” lên cuối. Độ phức tạp: Trung bình / Tệ nhất: O(n²) Tốt nhất (mảng đã sort): O(n) Bộ nhớ: O(1), ổn định. Ví dụ: Mảng: [5, 1, 4, 2] Lượt 1: (5,1) → swap → [1,5,4,2]; (5,4) → [1,4,5,2]; (5,2) → [1,4,2,5] Lượt 2: (1,4), ok; (4,2) → [1,2,4,5]; (4,5) ok → Mảng đã sort. public static void bubbleSort(int[] a) { boolean swapped; int n = a.length; do { swapped = false; for (int i = 1; i &lt; n; i++) { if (a[i - 1] &gt; a[i]) { int tmp = a[i]; a[i] = a[i - 1]; a[i - 1] = tmp; swapped = true; } } n--; // phần tử cuối đã đúng vị trí } while (swapped); } 2. Selection Sort Ý tưởng: Ở mỗi vòng lặp, tìm phần tử nhỏ nhất trong đoạn chưa sort, đưa nó về đầu. Sau vòng thứ i, các phần tử [0..i] đã đúng chỗ. Độ phức tạp: Trung bình / Tệ nhất: O(n²) Không ổn định, bộ nhớ O(1). Ví dụ: Mảng [29, 10, 14, 37] Lượt 1: min=10 → swap với 29 → [10,29,14,37] Lượt 2: min=14 → swap với 29 → [10,14,29,37] → Done. public static void selectionSort(int[] a) { int n = a.length; for (int i = 0; i &lt; n - 1; i++) { int min = i; for (int j = i + 1; j &lt; n; j++) { if (a[j] &lt; a[min]) min = j; } int tmp = a[i]; a[i] = a[min]; a[min] = tmp; } } 3. Insertion Sort Ý tưởng: Duyệt từ trái sang phải. Với mỗi phần tử, chèn nó vào đúng vị trí trong đoạn đã sort trước đó. Độ phức tạp: Trung bình / Tệ nhất: O(n²) Tốt nhất (mảng gần như sort): O(n) Ổn định, bộ nhớ O(1). Ví dụ: Mảng [5, 3, 4] 5: đoạn đã sort [5]. 3: chèn vào trước 5 → [3,5]. 4: chèn giữa 3 và 5 → [3,4,5]. public static void insertionSort(int[] a) { for (int i = 1; i &lt; a.length; i++) { int key = a[i]; int j = i - 1; while (j &gt;= 0 &amp;&amp; a[j] &gt; key) { a[j + 1] = a[j]; j--; } a[j + 1] = key; } } 4. Merge Sort Ý tưởng: Chia để trị: Chia mảng thành 2 nửa. Sắp xếp từng nửa đệ quy. Gộp (merge) 2 nửa đã sort. Độ phức tạp: Luôn O(n log n) Bộ nhớ: O(n), ổn định. Ví dụ: Mảng [38, 27, 43, 3] Chia: [38,27] và [43,3]. Sort trái → [27,38]; sort phải → [3,43]. Merge → [3,27,38,43]. public static void mergeSort(int[] a) { if (a.length &lt;= 1) return; int[] tmp = new int[a.length]; mergeSort(a, 0, a.length - 1, tmp); } private static void mergeSort(int[] a, int l, int r, int[] tmp) { if (l &gt;= r) return; int m = l + (r - l) / 2; mergeSort(a, l, m, tmp); mergeSort(a, m + 1, r, tmp); merge(a, l, m, r, tmp); } private static void merge(int[] a, int l, int m, int r, int[] tmp) { int i = l, j = m + 1, k = l; while (i &lt;= m &amp;&amp; j &lt;= r) { if (a[i] &lt;= a[j]) tmp[k++] = a[i++]; else tmp[k++] = a[j++]; } while (i &lt;= m) tmp[k++] = a[i++]; while (j &lt;= r) tmp[k++] = a[j++]; for (int t = l; t &lt;= r; t++) a[t] = tmp[t]; } 5. Quick Sort (chia để trị) Ý tưởng: Chia để trị: Chọn pivot (thường là giữa, trái, phải hoặc ngẫu nhiên). Phân chia mảng thành 2 phần: ≤ pivot và ≥ pivot. Đệ quy sort 2 nửa. Độ phức tạp: Trung bình: O(n log n) Tệ nhất: O(n²) (pivot xấu). Bộ nhớ: O(log n), không ổn định. Ví dụ: Mảng [9,3,7,1], pivot=7 Nhỏ hơn pivot: [3,1], lớn hơn: [9]. Sort [3,1] → [1,3]. Merge lại: [1,3,7,9]. public static void quickSort(int[] a) { quickSort(a, 0, a.length - 1); } private static void quickSort(int[] a, int l, int r) { if (l &gt;= r) return; int pivot = a[l + (r - l) / 2]; int i = l, j = r; while (i &lt;= j) { while (a[i] &lt; pivot) i++; while (a[j] &gt; pivot) j--; if (i &lt;= j) { int tmp = a[i]; a[i] = a[j]; a[j] = tmp; i++; j--; } } if (l &lt; j) quickSort(a, l, j); if (i &lt; r) quickSort(a, i, r); } 6. Heap Sort Ý tưởng: Xây max-heap từ mảng. Lặp: swap root (max) với phần tử cuối, giảm heap-size, heapify lại. Độ phức tạp: Luôn O(n log n) In-place, không ổn định. Ví dụ: Mảng [4,10,3,5,1] Heapify → [10,5,3,4,1] Swap 10 ↔ 1 → [1,5,3,4,10] Heapify → [5,4,3,1,10] Swap 5 ↔ 1 → [1,4,3,5,10] → v.v. public static void heapSort(int[] a) { int n = a.length; for (int i = n / 2 - 1; i &gt;= 0; i--) heapify(a, n, i); for (int end = n - 1; end &gt; 0; end--) { int tmp = a[0]; a[0] = a[end]; a[end] = tmp; heapify(a, end, 0); } } private static void heapify(int[] a, int n, int i) { int largest = i, left = 2*i + 1, right = 2*i + 2; if (left &lt; n &amp;&amp; a[left] &gt; a[largest]) largest = left; if (right &lt; n &amp;&amp; a[right] &gt; a[largest]) largest = right; if (largest != i) { int tmp = a[i]; a[i] = a[largest]; a[largest] = tmp; heapify(a, n, largest); } } 7. Counting Sort Ý tưởng: Đếm tần suất xuất hiện của từng số trong [min..max]. Tính prefix sum để biết vị trí. Sắp xếp ổn định bằng cách chèn ngược. Độ phức tạp: O(n + k), k = phạm vi giá trị. Bộ nhớ O(n+k), ổn định. 8. Radix Sort (áp dụng cho dữ liệu số nguyên lớn) Ý tưởng: Sort theo từng chữ số (hàng đơn vị → chục → trăm). Dùng Counting Sort ổn định ở mỗi bước. Độ phức tạp: O(d·(n + b)), với d = số chữ số, b = cơ số (10). Ổn định, tốt cho số nguyên lớn, không âm.]]></summary></entry><entry><title type="html">Kiến thức về Docker</title><link href="http://0.0.0.0:4000/2025/08/16/docker-post.html" rel="alternate" type="text/html" title="Kiến thức về Docker" /><published>2025-08-16T00:00:00-05:00</published><updated>2025-08-16T00:00:00-05:00</updated><id>http://0.0.0.0:4000/2025/08/16/docker-post</id><content type="html" xml:base="http://0.0.0.0:4000/2025/08/16/docker-post.html"><![CDATA[<p><strong>I. Kiến thức cơ bản về Docker?</strong></p>

<p><strong>1. Docker là gì</strong></p>

<ul>
  <li>Docker là một nền tảng để tạo, triển khai và chạy ứng dụng trong container.</li>
  <li>Container đóng gói ứng dụng và tất cả dependencies (thư viện, cấu hình) để chạy đồng nhất trên mọi môi trường.</li>
  <li>So với VM (Virtual Machine):
    <ul>
      <li>VM có OS riêng → nặng hơn.</li>
      <li>Container dùng chung kernel → nhẹ, nhanh, khởi động nhanh hơn.</li>
    </ul>
  </li>
</ul>

<p><strong>2. Thành phần chính của Docker</strong></p>

<ul>
  <li>Docker Engine: Nền tảng chạy container.</li>
  <li>Docker Image: File template dùng để tạo container. (VD: nginx:latest)</li>
  <li>Docker Container: Thực thể chạy từ image.</li>
  <li>Dockerfile: File mô tả cách build image.</li>
  <li>Docker Hub / Registry: Nơi lưu trữ image.</li>
  <li>Docker Compose: Tool để chạy nhiều container cùng lúc (multi-container).</li>
</ul>

<p><strong>3. Nguyên tắc container</strong></p>

<ul>
  <li>Immutable: Container không nên chỉnh sửa trực tiếp; nếu cần thay đổi → tạo image mới.</li>
  <li>Ephemeral: Container có thể bị xóa và tạo lại.</li>
  <li>Isolation: Container chạy độc lập về process, network, file system.</li>
</ul>

<p><strong>II. Các lệnh Docker cơ bản</strong></p>

<ol>
  <li>
    <p>Quản lý image</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker pull nginx:latest       # Tải image từ Docker Hub
docker images                  # Liệt kê image đã có
docker rmi &lt;image_id&gt;          # Xóa image
docker build -t myapp:v1 .     # Build image từ Dockerfile
</code></pre></div>    </div>
  </li>
  <li>
    <p>Quản lý container</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docker run -d --name mynginx -p 8080:80 nginx:latest   # Chạy container
docker ps                    # Xem container đang chạy
docker ps -a                 # Xem tất cả container
docker stop mynginx          # Dừng container
docker start mynginx         # Khởi động container đã dừng
docker rm mynginx            # Xóa container
docker logs mynginx          # Xem log container
docker exec -it mynginx bash # Vào bash container
</code></pre></div>    </div>
  </li>
  <li>
    <p>Docker Compose</p>
  </li>
</ol>

<ul>
  <li>
    <p>docker-compose.yml định nghĩa nhiều container, network, volume.</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  version: "3"
  services:
    web:
      image: nginx:latest
      ports:
        - "8080:80"
    db:
      image: mysql:8
      environment:
        MYSQL_ROOT_PASSWORD: example
</code></pre></div>    </div>
  </li>
  <li>
    <p>Lệnh thường dùng:</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  docker-compose up -d    # Chạy tất cả service
  docker-compose down     # Dừng và xóa
  docker-compose logs -f  # Xem log
</code></pre></div>    </div>
  </li>
</ul>

<p><strong>III. Docker trong thực tế</strong></p>

<ol>
  <li>Triển khai ứng dụng</li>
</ol>

<ul>
  <li>Backend Spring Boot, Node.js, Python:
    <ul>
      <li>Tạo Dockerfile.</li>
      <li>Build image → push lên registry → deploy container.</li>
    </ul>
  </li>
  <li>Frontend:
    <ul>
      <li>Docker dùng Nginx serve SPA.</li>
      <li>Mount code vào container để dev nhanh.</li>
    </ul>
  </li>
</ul>

<p>b. Docker và môi trường</p>

<ul>
  <li>Dev / Test / Prod:
    <ul>
      <li>Sử dụng cùng image → tránh lỗi “ở máy chạy được mà prod không chạy”.</li>
      <li>Docker Compose quản lý nhiều service.</li>
    </ul>
  </li>
</ul>

<p>c. Persistent Data</p>

<ul>
  <li>
    <p>Volume: dùng lưu dữ liệu, không bị mất khi container xóa.</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  docker run -v /host/path:/container/path nginx
</code></pre></div>    </div>
  </li>
</ul>

<p>d. Networking</p>

<ul>
  <li>Docker tạo network riêng cho container.</li>
  <li>Container có thể ping nhau bằng tên service.</li>
</ul>

<p><strong>IV. Best practices(Ưu điểm)</strong></p>

<ul>
  <li>Image nhẹ → dùng alpine thay vì ubuntu nếu không cần.</li>
  <li>Dockerfile: build multi-stage để giảm size.</li>
  <li>Không lưu dữ liệu trong container → dùng volume.</li>
  <li>Quản lý environment: .env file hoặc docker-compose.override.yml.</li>
  <li>Chỉ expose port cần thiết.</li>
  <li>Container không nên chạy root user.</li>
</ul>]]></content><author><name>Nguyen Phuc Ngoc Thanh</name></author><category term="docker" /><category term="devops" /><summary type="html"><![CDATA[I. Kiến thức cơ bản về Docker? 1. Docker là gì Docker là một nền tảng để tạo, triển khai và chạy ứng dụng trong container. Container đóng gói ứng dụng và tất cả dependencies (thư viện, cấu hình) để chạy đồng nhất trên mọi môi trường. So với VM (Virtual Machine): VM có OS riêng → nặng hơn. Container dùng chung kernel → nhẹ, nhanh, khởi động nhanh hơn. 2. Thành phần chính của Docker Docker Engine: Nền tảng chạy container. Docker Image: File template dùng để tạo container. (VD: nginx:latest) Docker Container: Thực thể chạy từ image. Dockerfile: File mô tả cách build image. Docker Hub / Registry: Nơi lưu trữ image. Docker Compose: Tool để chạy nhiều container cùng lúc (multi-container). 3. Nguyên tắc container Immutable: Container không nên chỉnh sửa trực tiếp; nếu cần thay đổi → tạo image mới. Ephemeral: Container có thể bị xóa và tạo lại. Isolation: Container chạy độc lập về process, network, file system. II. Các lệnh Docker cơ bản Quản lý image docker pull nginx:latest # Tải image từ Docker Hub docker images # Liệt kê image đã có docker rmi &lt;image_id&gt; # Xóa image docker build -t myapp:v1 . # Build image từ Dockerfile Quản lý container docker run -d --name mynginx -p 8080:80 nginx:latest # Chạy container docker ps # Xem container đang chạy docker ps -a # Xem tất cả container docker stop mynginx # Dừng container docker start mynginx # Khởi động container đã dừng docker rm mynginx # Xóa container docker logs mynginx # Xem log container docker exec -it mynginx bash # Vào bash container Docker Compose docker-compose.yml định nghĩa nhiều container, network, volume. version: "3" services: web: image: nginx:latest ports: - "8080:80" db: image: mysql:8 environment: MYSQL_ROOT_PASSWORD: example Lệnh thường dùng: docker-compose up -d # Chạy tất cả service docker-compose down # Dừng và xóa docker-compose logs -f # Xem log III. Docker trong thực tế Triển khai ứng dụng Backend Spring Boot, Node.js, Python: Tạo Dockerfile. Build image → push lên registry → deploy container. Frontend: Docker dùng Nginx serve SPA. Mount code vào container để dev nhanh. b. Docker và môi trường Dev / Test / Prod: Sử dụng cùng image → tránh lỗi “ở máy chạy được mà prod không chạy”. Docker Compose quản lý nhiều service. c. Persistent Data Volume: dùng lưu dữ liệu, không bị mất khi container xóa. docker run -v /host/path:/container/path nginx d. Networking Docker tạo network riêng cho container. Container có thể ping nhau bằng tên service. IV. Best practices(Ưu điểm) Image nhẹ → dùng alpine thay vì ubuntu nếu không cần. Dockerfile: build multi-stage để giảm size. Không lưu dữ liệu trong container → dùng volume. Quản lý environment: .env file hoặc docker-compose.override.yml. Chỉ expose port cần thiết. Container không nên chạy root user.]]></summary></entry><entry><title type="html">Tổng hợp kiến thức về Git</title><link href="http://0.0.0.0:4000/2025/07/18/git-post.html" rel="alternate" type="text/html" title="Tổng hợp kiến thức về Git" /><published>2025-07-18T00:00:00-05:00</published><updated>2025-07-18T00:00:00-05:00</updated><id>http://0.0.0.0:4000/2025/07/18/git-post</id><content type="html" xml:base="http://0.0.0.0:4000/2025/07/18/git-post.html"><![CDATA[<p><strong>I. Git là gì?</strong></p>

<ul>
  <li>Git là hệ thống quản lý phiên bản phân tán (Distributed Version Control System).</li>
  <li>Mỗi người clone repository sẽ có toàn bộ lịch sử thay đổi (không phụ thuộc vào kết nối với server như SVN).</li>
  <li>Git giúp theo dõi thay đổi mã nguồn, làm việc nhóm hiệu quả, rollback khi cần.</li>
</ul>

<p><strong>II. Kiến trúc cơ bản</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    +----------------------+
    |  Working Directory   |  &lt;-- Thư mục làm việc
    +----------------------+
              |
              | git add
              v
    +----------------------+
    |   Staging Area       |  &lt;-- File được đánh dấu để commit (lưu ở .git/index)
    +----------------------+
              |
              | git commit
              v
    +----------------------+
    | Local Repository     | &lt;-- Commit được lưu vào máy (trong .git/objects)
    +----------------------+
              |
              | git push
              v
    +----------------------+
    | Remote Repository    | &lt;-- Lưu trữ trên GitHub, GitLab, Bitbucket, v.v.
    +----------------------+
</code></pre></div></div>

<ul>
  <li>Working Directory: thư mục làm việc.</li>
  <li>Staging Area (Index): nơi lưu trữ tạm thời các thay đổi chuẩn bị commit.</li>
  <li>Local Repository: nơi lưu trữ các commit đã thực hiện.</li>
  <li>Remote Repository: ví dụ như GitHub, GitLab, Bitbucket.</li>
</ul>

<p><strong>III. Các câu lệnh Git</strong></p>

<p><strong>1. Lệnh git cơ bản</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    |----------------------------------------------------------|
    | Lệnh                    | Chức năng                      |
    |----------------------------------------------------------|
    | git init                | Tạo repo Git mới               |
    | git clone &lt;url&gt;         | Tải repo từ remote về          |
    | git status              | Xem trạng thái thay đổi        |
    | git add &lt;file&gt;          | Thêm file vào Staging Area     |
    | git add .               | Thêm tất cả file thay đổi      |
    | git commit -m "message" | Tạo commit                     |
    | git log                 | Xem lịch sử commit             |
    | git diff                | So sánh thay đổi chưa commit   |
    | git checkout &lt;branch&gt;   | Chuyển nhánh                   |
    | git branch              | Liệt kê danh sách nhánh        |
    | git branch &lt;name&gt;       | Tạo nhánh mới                  |
    | git merge &lt;branch&gt;      | Gộp nhánh                      |
    | git pull                | Lấy code mới nhất từ remote    |
    | git push                | Đẩy code lên remote            |
</code></pre></div></div>

<p><strong>2. Làm việc với Branch (nhánh)</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    git checkout -b feature/login    # Tạo và chuyển sang nhánh mới
    git checkout main                # Quay về nhánh chính
    git merge feature/login          # Gộp nhánh vào nhánh hiện tại
</code></pre></div></div>

<p><strong>3. Revert, Reset, Amend</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    |----------------------------------------------------------------------|
    | Lệnh                    | Ý nghĩa                                    |
    |----------------------------------------------------------------------|
    | git reset               | Quay lại commit trước đó (có thể mất code) |
    | git reset --soft HEAD~1 | Quay lại nhưng vẫn giữ thay đổi            |
    | git reset --hard HEAD~1 | Quay lại và xóa luôn thay đổi              |
    | git revert &lt;commit&gt;     | Tạo commit mới để đảo ngược commit cũ      |
    | git commit --amend      | Sửa lại commit gần nhất                    |
</code></pre></div></div>

<p><strong>4. Stash – Tạm thời lưu thay đổi</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    git stash               # Lưu tạm thay đổi
    git stash list          # Xem danh sách stash
    git stash apply         # Áp dụng lại thay đổi
    git stash drop          # Xóa stash
</code></pre></div></div>

<p><strong>5. Remote Repository (GitHub, GitLab)</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    git remote -v                     # Kiểm tra remote
    git remote add origin &lt;url&gt;      # Thêm remote repo
    git push -u origin main          # Push lần đầu
    git pull origin main             # Lấy code mới
</code></pre></div></div>

<p><strong>6. Giải quyết Conflict (xung đột)</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>- Khi merge hoặc pull gây xung đột, Git sẽ đánh dấu các phần xung đột:

      
      &lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD
      # Code của bạn
      Code của người khác
      &gt; &gt; &gt; &gt; &gt; &gt; &gt; branch-name
      

- Sửa file thủ công, sau đó:

      
      git add .
      git commit -m "resolve conflict"
</code></pre></div></div>

<p><strong>7. Git Flow - Quy trình làm việc chuẩn</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>a. main – nhánh sản phẩm chạy chính thức.
b. develop – nhánh phát triển chung.
c. feature/\* – nhánh tính năng.
d. release/\* – nhánh chuẩn bị release.
e. hotfix/\* – xử lý lỗi khẩn cấp.
</code></pre></div></div>

<p><strong>IV. Các câu hỏi phỏng vấn thường gặp</strong></p>

<p><em>1. Git khác gì SVN?</em></p>

<p><em>2. Sự khác nhau giữa git reset, git revert, git checkout?</em></p>

<p><em>3. Khi nào dùng git merge vs git rebase?</em></p>

<p><em>4. Cách giải quyết conflict?</em></p>

<p><em>5. Quy trình deploy/CI với Git?</em></p>]]></content><author><name>Nguyen Phuc Ngoc Thanh</name></author><summary type="html"><![CDATA[I. Git là gì? Git là hệ thống quản lý phiên bản phân tán (Distributed Version Control System). Mỗi người clone repository sẽ có toàn bộ lịch sử thay đổi (không phụ thuộc vào kết nối với server như SVN). Git giúp theo dõi thay đổi mã nguồn, làm việc nhóm hiệu quả, rollback khi cần. II. Kiến trúc cơ bản +----------------------+ | Working Directory | &lt;-- Thư mục làm việc +----------------------+ | | git add v +----------------------+ | Staging Area | &lt;-- File được đánh dấu để commit (lưu ở .git/index) +----------------------+ | | git commit v +----------------------+ | Local Repository | &lt;-- Commit được lưu vào máy (trong .git/objects) +----------------------+ | | git push v +----------------------+ | Remote Repository | &lt;-- Lưu trữ trên GitHub, GitLab, Bitbucket, v.v. +----------------------+ Working Directory: thư mục làm việc. Staging Area (Index): nơi lưu trữ tạm thời các thay đổi chuẩn bị commit. Local Repository: nơi lưu trữ các commit đã thực hiện. Remote Repository: ví dụ như GitHub, GitLab, Bitbucket. III. Các câu lệnh Git 1. Lệnh git cơ bản |----------------------------------------------------------| | Lệnh | Chức năng | |----------------------------------------------------------| | git init | Tạo repo Git mới | | git clone &lt;url&gt; | Tải repo từ remote về | | git status | Xem trạng thái thay đổi | | git add &lt;file&gt; | Thêm file vào Staging Area | | git add . | Thêm tất cả file thay đổi | | git commit -m "message" | Tạo commit | | git log | Xem lịch sử commit | | git diff | So sánh thay đổi chưa commit | | git checkout &lt;branch&gt; | Chuyển nhánh | | git branch | Liệt kê danh sách nhánh | | git branch &lt;name&gt; | Tạo nhánh mới | | git merge &lt;branch&gt; | Gộp nhánh | | git pull | Lấy code mới nhất từ remote | | git push | Đẩy code lên remote | 2. Làm việc với Branch (nhánh) git checkout -b feature/login # Tạo và chuyển sang nhánh mới git checkout main # Quay về nhánh chính git merge feature/login # Gộp nhánh vào nhánh hiện tại 3. Revert, Reset, Amend |----------------------------------------------------------------------| | Lệnh | Ý nghĩa | |----------------------------------------------------------------------| | git reset | Quay lại commit trước đó (có thể mất code) | | git reset --soft HEAD~1 | Quay lại nhưng vẫn giữ thay đổi | | git reset --hard HEAD~1 | Quay lại và xóa luôn thay đổi | | git revert &lt;commit&gt; | Tạo commit mới để đảo ngược commit cũ | | git commit --amend | Sửa lại commit gần nhất | 4. Stash – Tạm thời lưu thay đổi git stash # Lưu tạm thay đổi git stash list # Xem danh sách stash git stash apply # Áp dụng lại thay đổi git stash drop # Xóa stash 5. Remote Repository (GitHub, GitLab) git remote -v # Kiểm tra remote git remote add origin &lt;url&gt; # Thêm remote repo git push -u origin main # Push lần đầu git pull origin main # Lấy code mới 6. Giải quyết Conflict (xung đột) - Khi merge hoặc pull gây xung đột, Git sẽ đánh dấu các phần xung đột: &lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD # Code của bạn Code của người khác &gt; &gt; &gt; &gt; &gt; &gt; &gt; branch-name - Sửa file thủ công, sau đó: git add . git commit -m "resolve conflict" 7. Git Flow - Quy trình làm việc chuẩn a. main – nhánh sản phẩm chạy chính thức. b. develop – nhánh phát triển chung. c. feature/\* – nhánh tính năng. d. release/\* – nhánh chuẩn bị release. e. hotfix/\* – xử lý lỗi khẩn cấp. IV. Các câu hỏi phỏng vấn thường gặp 1. Git khác gì SVN? 2. Sự khác nhau giữa git reset, git revert, git checkout? 3. Khi nào dùng git merge vs git rebase? 4. Cách giải quyết conflict? 5. Quy trình deploy/CI với Git?]]></summary></entry><entry><title type="html">Ôn tập kiến thức về Cấu trúc dữ liệu</title><link href="http://0.0.0.0:4000/2025/07/17/data-structures-post.html" rel="alternate" type="text/html" title="Ôn tập kiến thức về Cấu trúc dữ liệu" /><published>2025-07-17T00:00:00-05:00</published><updated>2025-07-17T00:00:00-05:00</updated><id>http://0.0.0.0:4000/2025/07/17/data-structures-post</id><content type="html" xml:base="http://0.0.0.0:4000/2025/07/17/data-structures-post.html"><![CDATA[<p><strong>I. Cấu trúc Dữ liệu là gì?</strong></p>

<ul>
  <li>Cấu trúc dữ liệu là cách tổ chức, lưu trữ và quản lý dữ liệu để có thể truy xuất và xử lý hiệu quả.</li>
  <li>Là nền tảng cho thuật toán hoạt động hiệu quả về thời gian (time) và bộ nhớ (space).</li>
</ul>

<p><strong>II. Phân loại chính</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    |-------------------------------------------|------------------------------------------|
    | Nhóm                    | Loại            | Đặc điểm                                 |
    |-------------------------------------------|------------------------------------------|
    | Tuyến tính (Linear)     | Array (mảng)    | Truy cập chỉ số, lưu trữ cố định         |
    |                         | Linked List     | Danh sách liên kết các node              |
    |                         | Stack           | LIFO – Last In First Out                 |
    |                         | Queue           | FIFO – First In First Out                |
    | Phi tuyến (Non-linear)  | Tree            | Cấu trúc phân cấp gồm node cha – con     |
    |                         | Graph           | Mô hình đỉnh và cạnh                     |
    | Cấu trúc khác           | Hash Table      | Bảng băm, ánh xạ key → value             |
</code></pre></div></div>

<p><strong>III. Phân tích từng cấu trúc</strong></p>

<ol>
  <li>
    <p>Array (Mảng)
a. Khái niệm</p>

    <ul>
      <li>Array (mảng) là một cấu trúc dữ liệu tuyến tính, lưu trữ tập hợp các phần tử cùng kiểu dữ liệu, đặt cạnh nhau trong bộ nhớ.</li>
      <li>Các phần tử trong mảng được truy cập thông qua chỉ số (index), thường bắt đầu từ 0.</li>
      <li>
        <p>Ví dụ:</p>

        <p>int[] arr = {10, 20, 30, 40, 50};
System.out.println(arr[2]); // Kết quả: 30</p>
      </li>
    </ul>

    <p>b. Đặc điểm</p>

    <ul>
      <li>Truy cập ngẫu nhiên (Random Access): Do các phần tử được lưu liền kề, nên việc truy cập phần tử theo index có độ phức tạp O(1).</li>
      <li>Kích thước cố định: Một mảng tĩnh có kích thước không thể thay đổi sau khi khai báo (trừ khi dùng cấu trúc động như ArrayList trong Java hoặc vector trong C++).</li>
      <li>Đồng nhất: Các phần tử trong mảng có cùng kiểu dữ liệu.</li>
    </ul>

    <p>c. Ưu điểm</p>

    <ul>
      <li>Truy cập nhanh: Tìm phần tử theo chỉ số cực nhanh → O(1).</li>
      <li>Đơn giản: Dễ khai báo, dễ sử dụng.</li>
      <li>Tối ưu bộ nhớ cache: Do các phần tử lưu liền kề nên tận dụng CPU cache tốt.</li>
    </ul>

    <p>d. Nhược điểm</p>

    <ul>
      <li>Kích thước cố định: Không thể thay đổi sau khi khai báo (trong ngôn ngữ như C, Java).</li>
      <li>Chi phí chèn/xoá cao:
        <ul>
          <li>Xoá hoặc thêm 1 phần tử giữa mảng phải dịch chuyển các phần tử còn lại → O(n).</li>
          <li>Thêm cuối mảng có thể nhanh (O(1)) nếu còn chỗ, nhưng nếu đầy thì phải cấp phát lại (trong mảng động).</li>
        </ul>
      </li>
      <li>Lãng phí bộ nhớ: Nếu khai báo kích thước lớn mà dùng ít → dư thừa.</li>
    </ul>

    <p>e. Các thao tác cơ bản và độ phức tạp</p>

    <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>|--------------------------------------------|
| Thao tác                    | Độ phức tạp  |
|--------------------------------------------|
| Truy cập arr[i]             | O(1)         |
| Cập nhật arr[i] = x         | O(1)         |
| Tìm kiếm tuyến tính         | O(n)         |
| Thêm/Xoá cuối (nếu còn chỗ) | O(1)         |
| Thêm/Xoá giữa mảng          | O(n)         |
</code></pre></div>    </div>
  </li>
  <li>
    <p>Linked List</p>
  </li>
  <li>
    <p>Stack</p>
  </li>
  <li>
    <p>Queue</p>
  </li>
  <li>
    <p>Hash Table / HashMap</p>
  </li>
  <li>
    <p>Tree (Cây)</p>
  </li>
  <li>
    <p>Graph (Đồ thị)</p>
  </li>
</ol>

<p><strong>III. Các câu hỏi thường gặp trong phỏng vấn</strong></p>

<ol>
  <li>So sánh Array vs Linked List</li>
  <li>So sánh Array vs ArrayList (Java)</li>
</ol>]]></content><author><name>Nguyen Phuc Ngoc Thanh</name></author><summary type="html"><![CDATA[I. Cấu trúc Dữ liệu là gì? Cấu trúc dữ liệu là cách tổ chức, lưu trữ và quản lý dữ liệu để có thể truy xuất và xử lý hiệu quả. Là nền tảng cho thuật toán hoạt động hiệu quả về thời gian (time) và bộ nhớ (space). II. Phân loại chính |-------------------------------------------|------------------------------------------| | Nhóm | Loại | Đặc điểm | |-------------------------------------------|------------------------------------------| | Tuyến tính (Linear) | Array (mảng) | Truy cập chỉ số, lưu trữ cố định | | | Linked List | Danh sách liên kết các node | | | Stack | LIFO – Last In First Out | | | Queue | FIFO – First In First Out | | Phi tuyến (Non-linear) | Tree | Cấu trúc phân cấp gồm node cha – con | | | Graph | Mô hình đỉnh và cạnh | | Cấu trúc khác | Hash Table | Bảng băm, ánh xạ key → value | III. Phân tích từng cấu trúc Array (Mảng) a. Khái niệm Array (mảng) là một cấu trúc dữ liệu tuyến tính, lưu trữ tập hợp các phần tử cùng kiểu dữ liệu, đặt cạnh nhau trong bộ nhớ. Các phần tử trong mảng được truy cập thông qua chỉ số (index), thường bắt đầu từ 0. Ví dụ: int[] arr = {10, 20, 30, 40, 50}; System.out.println(arr[2]); // Kết quả: 30 b. Đặc điểm Truy cập ngẫu nhiên (Random Access): Do các phần tử được lưu liền kề, nên việc truy cập phần tử theo index có độ phức tạp O(1). Kích thước cố định: Một mảng tĩnh có kích thước không thể thay đổi sau khi khai báo (trừ khi dùng cấu trúc động như ArrayList trong Java hoặc vector trong C++). Đồng nhất: Các phần tử trong mảng có cùng kiểu dữ liệu. c. Ưu điểm Truy cập nhanh: Tìm phần tử theo chỉ số cực nhanh → O(1). Đơn giản: Dễ khai báo, dễ sử dụng. Tối ưu bộ nhớ cache: Do các phần tử lưu liền kề nên tận dụng CPU cache tốt. d. Nhược điểm Kích thước cố định: Không thể thay đổi sau khi khai báo (trong ngôn ngữ như C, Java). Chi phí chèn/xoá cao: Xoá hoặc thêm 1 phần tử giữa mảng phải dịch chuyển các phần tử còn lại → O(n). Thêm cuối mảng có thể nhanh (O(1)) nếu còn chỗ, nhưng nếu đầy thì phải cấp phát lại (trong mảng động). Lãng phí bộ nhớ: Nếu khai báo kích thước lớn mà dùng ít → dư thừa. e. Các thao tác cơ bản và độ phức tạp |--------------------------------------------| | Thao tác | Độ phức tạp | |--------------------------------------------| | Truy cập arr[i] | O(1) | | Cập nhật arr[i] = x | O(1) | | Tìm kiếm tuyến tính | O(n) | | Thêm/Xoá cuối (nếu còn chỗ) | O(1) | | Thêm/Xoá giữa mảng | O(n) | Linked List Stack Queue Hash Table / HashMap Tree (Cây) Graph (Đồ thị) III. Các câu hỏi thường gặp trong phỏng vấn So sánh Array vs Linked List So sánh Array vs ArrayList (Java)]]></summary></entry><entry><title type="html">Tìm hiểu về Redis</title><link href="http://0.0.0.0:4000/2025/07/16/redis-post.html" rel="alternate" type="text/html" title="Tìm hiểu về Redis" /><published>2025-07-16T00:00:00-05:00</published><updated>2025-07-16T00:00:00-05:00</updated><id>http://0.0.0.0:4000/2025/07/16/redis-post</id><content type="html" xml:base="http://0.0.0.0:4000/2025/07/16/redis-post.html"><![CDATA[<p><strong>I. Tổng quan về Redis</strong></p>

<ul>
  <li>Redis là một cơ sở dữ liệu lưu trữ key-value trên RAM (in-memory), rất nhanh.</li>
  <li>Hỗ trợ các kiểu dữ liệu như: String, List, Set, Hash, Sorted Set,…</li>
  <li>Dùng cho: caching (bộ nhớ đệm), session, queue/pub-sub, ranking,…</li>
</ul>]]></content><author><name>Nguyen Phuc Ngoc Thanh</name></author><summary type="html"><![CDATA[I. Tổng quan về Redis Redis là một cơ sở dữ liệu lưu trữ key-value trên RAM (in-memory), rất nhanh. Hỗ trợ các kiểu dữ liệu như: String, List, Set, Hash, Sorted Set,… Dùng cho: caching (bộ nhớ đệm), session, queue/pub-sub, ranking,…]]></summary></entry><entry><title type="html">Tổng hợp kiến thức về Spring Testing</title><link href="http://0.0.0.0:4000/2025/07/14/spring-testing.html" rel="alternate" type="text/html" title="Tổng hợp kiến thức về Spring Testing" /><published>2025-07-14T00:00:00-05:00</published><updated>2025-07-14T00:00:00-05:00</updated><id>http://0.0.0.0:4000/2025/07/14/spring-testing</id><content type="html" xml:base="http://0.0.0.0:4000/2025/07/14/spring-testing.html"><![CDATA[]]></content><author><name>Nguyen Phuc Ngoc Thanh</name></author><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Tổng hợp kiến thức về Spring Cloud (Microservices - optional)</title><link href="http://0.0.0.0:4000/2025/07/13/spring-cloud.html" rel="alternate" type="text/html" title="Tổng hợp kiến thức về Spring Cloud (Microservices - optional)" /><published>2025-07-13T00:00:00-05:00</published><updated>2025-07-13T00:00:00-05:00</updated><id>http://0.0.0.0:4000/2025/07/13/spring-cloud</id><content type="html" xml:base="http://0.0.0.0:4000/2025/07/13/spring-cloud.html"><![CDATA[]]></content><author><name>Nguyen Phuc Ngoc Thanh</name></author><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Tổng hợp kiến thức về Spring WebFlux (Reactive Programming - optional)</title><link href="http://0.0.0.0:4000/2025/07/12/spring-webflux.html" rel="alternate" type="text/html" title="Tổng hợp kiến thức về Spring WebFlux (Reactive Programming - optional)" /><published>2025-07-12T00:00:00-05:00</published><updated>2025-07-12T00:00:00-05:00</updated><id>http://0.0.0.0:4000/2025/07/12/spring-webflux</id><content type="html" xml:base="http://0.0.0.0:4000/2025/07/12/spring-webflux.html"><![CDATA[]]></content><author><name>Nguyen Phuc Ngoc Thanh</name></author><summary type="html"><![CDATA[]]></summary></entry><entry><title type="html">Tổng hợp kiến thức về Spring Scheduler &amp;amp; Async</title><link href="http://0.0.0.0:4000/2025/07/11/spring-scheduler-and-async.html" rel="alternate" type="text/html" title="Tổng hợp kiến thức về Spring Scheduler &amp;amp; Async" /><published>2025-07-11T00:00:00-05:00</published><updated>2025-07-11T00:00:00-05:00</updated><id>http://0.0.0.0:4000/2025/07/11/spring-scheduler-and-async</id><content type="html" xml:base="http://0.0.0.0:4000/2025/07/11/spring-scheduler-and-async.html"><![CDATA[]]></content><author><name>Nguyen Phuc Ngoc Thanh</name></author><summary type="html"><![CDATA[]]></summary></entry></feed>